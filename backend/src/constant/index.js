const HTTP_STATUS = require("./http-status");
const HTTP_TEXT = require("./http-text");
const ACTION = require("./history-action");
const RESPONSES = require("./response-messages");

module.exports = {
  HTTP_STATUS,
  HTTP_TEXT,
  ACTION,
  RESPONSES,
};
